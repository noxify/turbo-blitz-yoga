{
  "name": "@acme/permissions",
  "version": "0.1.0",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf .turbo node_modules",
    "with-env": "dotenv -c -e ../../.env --",
    "test": "vitest run"
  },
  "dependencies": {
    "@acme/db": "*",
    "@casl/ability": "6.3.3"
  },
  "devDependencies": {
    "dotenv-cli": "7.0.0",
    "typescript": "4.9.5",
    "vitest": "0.28.5"
  }
}
